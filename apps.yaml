apps:
  jq:
    install: brew
    category: cli
    tier: auto
    description: JSON processor

  yq:
    install: brew
    category: cli
    tier: auto
    description: YAML processor

  btop:
    install: brew
    category: cli
    tier: auto
    description: Resource monitor

  bat:
    install: brew
    category: cli
    tier: auto
    description: Cat with syntax highlighting

  asdf:
    install: brew
    category: dev
    tier: auto
    description: Version manager (node, python, etc)
    zsh: |
      # asdf version manager
      . /opt/homebrew/opt/asdf/libexec/asdf.sh
