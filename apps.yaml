apps:
  jq:
    install: brew
    category: cli
    tier: auto
    description: JSON processor

  kind:
    install: brew
    category: cli
    description: Run local Kubernetes cluster in Docker
    depends:
      - docker

  yq:
    install: brew
    category: cli
    tier: auto
    description: YAML processor

  btop:
    install: brew
    category: cli
    tier: auto
    description: Resource monitor

  htop:
    install: brew
    category: cli
    tier: auto
    description: Improved top (interactive process viewer)

  bat:
    install: brew
    category: cli
    tier: auto
    description: Cat with syntax highlighting

  ripgrep:
    install: brew
    category: cli
    tier: auto
    description: Fast grep alternative (rg)

  fzf:
    install: brew
    category: cli
    tier: auto
    description: Fuzzy finder

  fd:
    install: brew
    category: cli
    tier: auto
    description: Fast find alternative

  eza:
    install: brew
    category: cli
    tier: auto
    description: Modern ls replacement

  zoxide:
    install: brew
    category: cli
    tier: auto
    description: Smarter cd command
    zsh: |
      eval "$(zoxide init zsh)"

  # TODO: needs python env manager (pyenv/uv) first
  # aws-cdk:
  #   install: brew
  #   category: cli
  #   tier: auto
  #   description: AWS Cloud Development Kit
  #   depends:
  #     - fnm
  #
  # awscli:
  #   install: brew
  #   category: cli
  #   tier: auto
  #   description: Official AWS command-line interface

  gh:
    install: brew
    category: cli
    tier: auto
    description: GitHub command-line tool

  helm:
    install: brew
    category: cli
    description: Kubernetes package manager

  gitui:
    install: brew
    category: cli
    tier: auto
    description: Blazing fast terminal UI for git

  glow:
    install: brew
    category: cli
    tier: auto
    description: Markdown viewer

  yazi:
    install: brew
    category: cli
    tier: auto
    description: Terminal file manager

  tmux:
    install: brew
    category: cli
    description: Terminal multiplexer

  uv:
    install: brew
    category: cli
    tier: auto
    description: Fast Python package manager

  watch:
    install: brew
    category: cli
    description: Execute program periodically with fullscreen output

  fnm:
    install: brew
    category: cli
    tier: required
    description: Fast Node.js version manager
    zsh: |
      # fnm - fast node manager
      eval "$(fnm env --use-on-cd)"
    post_install:
      - fnm install 24
      - fnm default 24

  mas:
    install: brew
    category: cli
    tier: required
    description: Mac App Store CLI

  # FIXME: not yet ready - npm install needs fnm setup first
  # claude-code:
  #   install: npm
  #   package: "@anthropic-ai/claude-code"
  #   category: cli
  #   tier: auto
  #   description: Claude Code CLI
  #   zsh: |
  #     alias claude-agent='claude --dangerously-skip-permissions'

  claude:
    install: cask
    category: apps
    tier: auto
    description: Claude macOS app

  daisydisk:
    install: cask
    category: apps
    description: Disk space visualizer

  localsend:
    install: cask
    category: apps
    description: Open-source cross-platform alternative to AirDrop

  rectangle:
    install: cask
    category: apps
    description: Move and resize windows using keyboard shortcuts or snap areas

  sf-symbols:
    install: cask
    category: apps
    description: Tool that provides consistent, highly configurable symbols for apps

  twingate:
    install: cask
    category: apps
    description: Zero trust network access platform

  windows-app:
    install: cask
    category: apps
    description: Connect to Windows

  # TODO: requires App Store sign-in
  # amphetamine:
  #   install: mas
  #   id: 937984704
  #   category: apps
  #   tier: auto
  #   description: Keep Mac awake
